// Aru deinitialization script

if sprite_exists( gir_stand_sprite )
  spr_delete( gir_stand_sprite );

if sprite_exists( gir_pig_sprite )
  spr_delete( gir_pig_sprite );
  
if sprite_exists( gir_land_sprite )
  spr_delete( gir_land_sprite );

if sprite_exists( gir_fall_sprite )
  spr_delete( gir_fall_sprite );

if sprite_exists( gir_shoot_sprite )
  spr_delete( gir_shoot_sprite );

if sprite_exists( gir_leave_sprite )
  spr_delete( gir_leave_sprite );