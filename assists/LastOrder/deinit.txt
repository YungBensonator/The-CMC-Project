// Kae deinitialization script

if sprite_exists( last_order_assist_stand )
  spr_delete( last_order_assist_stand );

if sprite_exists( last_order_assist_jump )
  spr_delete( last_order_assist_jump );

if sprite_exists( last_order_assist_run )
  spr_delete( last_order_assist_run );
  
if sprite_exists( last_order_assist_grab )
  spr_delete( last_order_assist_grab );