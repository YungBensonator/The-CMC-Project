if sprite_exists( sakura_start_sprite )
  spr_delete( sakura_start_sprite );

if sprite_exists( sakura_atk_sprite )
  spr_delete( sakura_atk_sprite );
  
if sprite_exists( sakura_effect_sprite )
  spr_delete( sakura_effect_sprite );