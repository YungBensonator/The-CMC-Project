// Aru deinitialization script

if sprite_exists( yuki_stand_sprite )
  spr_delete( yuki_stand_sprite );

if sprite_exists( yuki_disappear_sprite )
  spr_delete( yuki_disappear_sprite );
  
if sprite_exists( yuki_atk_sprite )
  spr_delete( yuki_atk_sprite );
  
if sprite_exists( yuki_bullet_sprite )
  spr_delete( yuki_bullet_sprite );