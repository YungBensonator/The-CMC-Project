// Roll deinitialization script

if sprite_exists( entry_sprite )
  spr_delete( entry_sprite );

if sprite_exists( exit_sprite )
  spr_delete( exit_sprite );

if sprite_exists( soporte_sprite )
  spr_delete( soporte_sprite );
  
if sprite_exists( coletas_sprite )
  spr_delete( coletas_sprite );
  
if sprite_exists( arrow_sprite )
  spr_delete( arrow_sprite );

if sprite_exists( magic_arrow_sprite )
  spr_delete( magic_arrow_sprite );