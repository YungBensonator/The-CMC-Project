if timer = 1 && hit = 0
{         // Initialize sprite and speed during frame 1
    visible = 1
	moving_object = 1;
}

if (collision_point(x+sign(hspeed),y,obj_wall,1,1) || is_on_floor(self)) && hit = 0
{
	attacker.id.timer = 110;
	instance_destroy();
}

if (!instance_exists(swordhit))
	swordhit = create_projectile(4,18+facing*144,4.85,3.5,6,5,slash2,obj_spark_slash1,1.1,0.56,0,0,attacker.id,1);