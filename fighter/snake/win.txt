player_frame[num] += 0.15; // Replace 0.25 with any value you deem appropriate.
if ( player_frame[num] >= 13 ) // Replace 6 with the number of frames Lose has.
  player_frame[num] = 13; // Same as above
  if ( player_timer[num] == 81 )
sound_play ( player_parent[num].win_voice );