image_alpha -= 0.025;
if ( temp = 0 ) image_xscale += 0.02;
if ( temp = 1 ) image_xscale -= 0.02;
image_yscale += 0.02;

if ( image_index >= image_number - 1 )
{
image_speed = 0;
}

if ( image_alpha <= 0 )
{
instance_destroy();
}